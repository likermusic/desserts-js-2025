{
  "name": "desserts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "json-server db.json --watch"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
